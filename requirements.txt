# --- Runtime: what Coast needs to run (main.py) ---
pyobjc-core>=11.0
pyobjc-framework-Cocoa>=11.0
pyobjc-framework-Quartz>=11.0

# --- Build only: packaging Coast.app / Coast.dmg (see build_dmg.sh) ---
pyinstaller>=6.0
